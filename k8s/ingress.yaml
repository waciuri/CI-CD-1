apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: techsolutions-ingress
  namespace: default
    # annotations:
    # kubernetes.io/ingress.class: "nginx"
      # nginx.ingress.kubernetes.io/rewrite-target: /$1
      # nginx.ingress.kubernetes.io/use-regex: "true"
      # nginx.ingress.kubernetes.io/ssl-redirect: "false"
      # cert-manager.io/cluster-issuer: "letsencrypt-prod"
      # metadata:
  annotations:
    nginx.ingress.kubernetes.io/rewrite-target: /$1
    nginx.ingress.kubernetes.io/use-regex: "true"
    nginx.ingress.kubernetes.io/ssl-redirect: "false"
    cert-manager.io/cluster-issuer: "letsencrypt-prod"
spec:
  ingressClassName: nginx
  tls:
  - hosts:
    - a289fd49bd80a4918bea08e465edcf76-c0dc7ee9f9cd4cf3.elb.us-east-1.amazonaws.com
    secretName: techsolutions-tls
  rules:
  - host: a289fd49bd80a4918bea08e465edcf76-c0dc7ee9f9cd4cf3.elb.us-east-1.amazonaws.com
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: techsolutions-service
            port:
              number: 80
  - http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: techsolutions-service
            port:
              number: 80
